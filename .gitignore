# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Tauri sidecar binaries (build artifacts / vendored executables)
src-tauri/binaries/

# Updater signing keypair — the public half is copied into tauri.conf.json's `plugins.updater.pubkey`
# by hand, so neither file here needs to be (or should be) committed.
.tauri-updater-keys/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
